import { FormulaSetupWizard } from "./formula-setup-wizard";

export const metadata = { title: "Create Formula — Olfacta" };

export default function CreateFormulaPage() {
  return <FormulaSetupWizard />;
}
